<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:layout_gravity="center"
    android:gravity="center"
    android:layout_marginBottom="@dimen/option_margin"
    android:paddingBottom="@dimen/option_margin"
    android:orientation="horizontal" >

    <Button
        android:id="@+id/optionTextButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:onClick="selectOption" />

    <ImageButton
        android:id="@+id/optionPictureButton"
        android:layout_width="@dimen/option_image"
        android:layout_height="@dimen/option_image"
        android:scaleType="centerInside"
        android:onClick="selectOption" />

</LinearLayout>