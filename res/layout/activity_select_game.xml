<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:orientation="vertical"
    android:gravity="center"
    tools:context=".SelectGameActivity" >

    <Button
        android:layout_width="@dimen/start_button"
        android:layout_height="@dimen/button_height"
        android:onClick="startQuestionsMenu"
        android:drawableLeft="@android:drawable/ic_menu_help"
        android:text="@string/start_questions_menu" />
    
    <Button
        android:layout_width="@dimen/start_button"
        android:layout_height="@dimen/button_height"
        android:text="@string/start_pecs_menu"
        android:drawableLeft="@android:drawable/ic_menu_gallery"
        android:onClick="startPecsMenu" />

</LinearLayout>